import '../auth/auth.handler'
import '../character/character.handler'
import '../spell/spell.handler'
import '../user/user.handler'

export const registerHandlers = () => {
  console.log( 'Registering handlers . . .' )
}
